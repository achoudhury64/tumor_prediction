# we are adding dummy value here as leaving it empty will throw error.
AUGMENTATION: True
IMAGE_SIZE: [224, 224, 3]
BATCH_SIZE: 16
INCLUDE_TOP: False
EPOCHS: 3
CLASSES: 2
WEIGHTS: imagenet
LEARNING_RATE: 0.02

#check the documentation of vgg16 here
#https://keras.io/api/applications/vgg/vgg_models/#vgg16-function
#